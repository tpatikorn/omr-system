================================================================================
                    OMR SYSTEM - คู่มือเริ่มต้นอย่างรวดเร็ว
================================================================================

📦 ไฟล์ที่ต้องมี (ทั้งหมด 5 ไฟล์)
================================================================================
✅ omr-system-image.tar          (ไฟล์ใหญ่ ~500-800 MB)
✅ docker-compose.prod.yml       
✅ .env.template                 
✅ start.ps1                     (สำหรับ Windows)
✅ start.sh                      (สำหรับ Mac/Linux)


🚀 ขั้นตอนติดตั้ง (3 ขั้นตอน)
================================================================================

ขั้นตอนที่ 1: ติดตั้ง Docker Desktop
------------------------------------
ดาวน์โหลดและติดตั้งจาก: https://www.docker.com/products/docker-desktop/


ขั้นตอนที่ 2: รัน Script
------------------------------------
Windows:  คลิกขวา start.ps1 → Run with PowerShell
Mac:      chmod +x start.sh && ./start.sh
Linux:    chmod +x start.sh && ./start.sh


ขั้นตอนที่ 3: ตั้งค่า IP
------------------------------------
1. หา IP ของเครื่อง:
   Windows: เปิด CMD พิมพ์ ipconfig
   Mac:     เปิด Terminal พิมพ์ ifconfig
   Linux:   เปิด Terminal พิมพ์ ip addr

2. แก้ไขไฟล์ .env (Script จะเปิดให้อัตโนมัติ)
   เปลี่ยน: OMR_BASE_URL=http://YOUR_IP_ADDRESS:5000
   เป็น:    OMR_BASE_URL=http://192.168.1.100:5000
   (ใส่ IP จริงของคุณ)

3. รัน Script อีกครั้ง


✅ เข้าใช้งาน
================================================================================
จากเครื่องเดียวกัน:     http://localhost:5000
จากเครื่องอื่น:          http://192.168.1.100:5000 (ใส่ IP ของคุณ)


🛠️ คำสั่งที่ใช้บ่อย
================================================================================
ดู Logs:        docker-compose -f docker-compose.prod.yml logs -f
หยุดระบบ:       docker-compose -f docker-compose.prod.yml down
เริ่มระบบ:      docker-compose -f docker-compose.prod.yml up -d
Restart:        docker-compose -f docker-compose.prod.yml restart
ดูสถานะ:        docker ps


🔧 แก้ปัญหาเบื้องต้น
================================================================================
ปัญหา: ไม่สามารถเข้าถึงจากเครื่องอื่น
แก้ไข: 
  1. ตรวจสอบ Firewall (เปิด port 5000)
  2. ตรวจสอบ IP ใน .env ถูกต้อง
  3. ตรวจสอบว่า Docker กำลังรัน: docker ps

ปัญหา: Port 5000 ถูกใช้งานอยู่
แก้ไข: แก้ไขไฟล์ docker-compose.prod.yml
  เปลี่ยน ports: - "5000:5000"
  เป็น   ports: - "5001:5000"


📖 คู่มือฉบับเต็ม
================================================================================
ดูรายละเอียดเพิ่มเติมในไฟล์: คู่มือติดตั้ง-OMR-System.md


📞 ติดปัญหา?
================================================================================
ดู logs เพื่อหาสาเหตุ: docker-compose -f docker-compose.prod.yml logs -f


================================================================================
                            ขอให้ใช้งานสนุก! 🎉
================================================================================
