# OMR System Configuration Template
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าให้ตรงกับเครื่องของคุณ

# ========================================
# วิธีหา IP Address ของเครื่อง:
# ========================================
# Windows: 
#   1. เปิด Command Prompt (CMD)
#   2. พิมพ์: ipconfig
#   3. มองหา "IPv4 Address" ในส่วน Wi-Fi หรือ Ethernet
#   4. ตัวอย่าง: 192.168.1.100
#
# Mac: 
#   1. เปิด Terminal
#   2. พิมพ์: ifconfig | grep "inet "
#   3. หรือดูที่ System Preferences → Network
#
# Linux:
#   1. เปิด Terminal
#   2. พิมพ์: ip addr show
#   3. หรือ: hostname -I
# ========================================

# Base URL - เปลี่ยน YOUR_IP_ADDRESS เป็น IP ของเครื่องคุณ
OMR_BASE_URL=http://YOUR_IP_ADDRESS:5000

# Server Configuration (ไม่ต้องแก้ไข)
SERVER_HOST=0.0.0.0
SERVER_PORT=5000

# Application Mode (ไม่ต้องแก้ไข)
FLASK_DEBUG=False
FLASK_ENV=production

# ========================================
# ตัวอย่างการตั้งค่า:
# ========================================
# ถ้า IP ของคุณคือ 192.168.1.100
# OMR_BASE_URL=http://192.168.1.100:5000
#
# ถ้าใช้แค่ในเครื่องเดียว (localhost)
# OMR_BASE_URL=http://localhost:5000
# ========================================
